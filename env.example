# Preferred: URL-encoded password (note %40 for @)
DATABASE_URL=postgresql://gaussdb:Secretpassword%40123@127.0.0.1:5432/postgres

# Or provide discrete parameters (overrides DATABASE_URL when all are present)
# PGHOST=127.0.0.1
# PGPORT=5432
# PGDATABASE=postgres
# PGUSER=gaussdb
# PGPASSWORD=Secretpassword@123

# Optional: number of generated rows
# NUM_VEHICLES=400
# NUM_CUSTOMERS=200

